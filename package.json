{
  "name": "sovereign-self",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Your digital identity. Your compute. Your rules.",
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "docker-compose up postgres -d && pnpm --filter agent dev & pnpm --filter api dev & pnpm --filter dashboard dev",
    "dev:dashboard": "pnpm --filter dashboard dev",
    "build": "pnpm --filter agent build && pnpm --filter api build && pnpm --filter dashboard build",
    "docker:up": "docker-compose up --build",
    "docker:down": "docker-compose down",
    "nosana:deploy": "nosana job post nosana/job.yaml",
    "seed:demo": "tsx packages/api/src/scripts/seedDemoData.ts"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
